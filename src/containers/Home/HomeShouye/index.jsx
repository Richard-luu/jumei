import { connect } from 'react-redux';
import Shouye from '@/components/Home/HomeShouye';

const mapStateToProps = (state, props) => {
  return {
  }
}

export default connect(
  mapStateToProps
)(Shouye);
